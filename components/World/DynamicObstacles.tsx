/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 *
 * DynamicObstacles - lightweight dev stub
 * Some environments (local/dev) may not include the full implementation.
 * Provide a safe no-op component to avoid import errors during dev/HMR.
 */

import React from 'react';

export const DynamicObstacles: React.FC = () => {
  // Minimal safe stub — real implementation lives in the full feature branch.
  return null;
};

export default DynamicObstacles;
