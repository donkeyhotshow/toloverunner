version: '3.8'

services:
  game-frontend:
    build: .
    ports:
      - "8080:80"
    environment:
      - VITE_APP_VERSION=2.4.0
    restart: always

  game-server:
    build: 
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "3000:3000"
    restart: always
